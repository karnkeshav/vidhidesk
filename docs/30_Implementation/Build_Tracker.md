> **Title:** VidhiDesk Build Tracker
> **Version:** v25 (internal versioning continues as before — see document footer)
> **Status:** Active — Canonical, living document
> **Owner:** Keshav
> **Audience:** Engineers, future AI agents, anyone needing ground-truth current build status
> **Last Updated:** 17 August 2026
> **Canonical Reference:** Yes, for current build/implementation status. Fulfills both "Sprint Tracker" and "Build Tracker" roles in this documentation hierarchy — no separate Sprint_Tracker.md exists.
> **Supersedes:** All prior status claims in `90_Historical/` documents
> **Related Documents:** [`30_Implementation/Backlog.md`](Backlog.md), [`30_Implementation/Technical_Design/Litigation_Module_Architecture.md`](Technical_Design/Litigation_Module_Architecture.md), [`00_Product/Roadmap.md`](../00_Product/Roadmap.md)

---

VidhiDesk — Master Build Tracker & Developer Handover
Consolidates: `UI/UX Design Notes` (design system) + `Stitch_Mockup_Plan.md` (what to mock up, in order) + `Navigation_and_Functional_Spec.md` (how every screen behaves/connects/talks to the backend), reconciled against the original `01_Scope_of_Work.md` / `02_Technical_Requirements.md` / `03_Implementation_Plan.md`, the revised `Project_Plan_Legal_AI_Assistant.md`, and actual implementation evidence pasted into chat since.
Document version: v18 — 5 August 2026 (updated after Advocate Profile endpoints implementation & test suite completion)
Status: Living tracker — update the Status column, and its evidence tag, as work lands.
---
0. How to use this document
Read §0.1 (evidence tagging system) first — every status claim in this document is tagged, and the tag changes what you're allowed to trust it for.
Read §0.2 (Google Stitch UI Governance) — mandatory 6-step lifecycle including Visual Design Review before implementation.
Read §0.3 (Official UI Signoff & Freeze State) — current approved design freeze status per module.
Read §1 (source docs + which plan wins where they disagree).
Read §3 (design system) once — it applies to every screen.
§5 is the actual tracker: Sprint → Session → Screens → Mockup ID → routes → API endpoints → DB tables → tagged Status.
§7 is "where we are right now," with a real evidence-backed build log — start here if you just want current state.
§8 lists gaps — some resolved since v1, some new, all tagged.
§11 (Evidence Log) is the numbered source list every `[E#]` citation in this document points to.
---
0.1 Evidence & Confidence Tagging System
Every architectural or status claim in this document carries one of five tags. This exists because "confirmed" already decayed once in this project — a clause was marked reviewed (`clause_reviews.review_status = 'kept'`), the underlying content was silently rewritten by re-seeding, and the "confirmed reviewed" status kept asserting something no longer true until a sweep caught it [E11]. Documentation can suffer the identical failure mode if status tags aren't bound to a specific piece of evidence and a date.
Tag	Meaning	Citation requirement
✅ Confirmed	Verified against implementation — code, migrations, git history, terminal output, database schema, API response	Must cite the specific evidence item `[E#]` and the date/commit it reflects. Decays — re-verify before relying on it if time has passed.
📐 Designed	Explicitly specified in SOW/TRD/Project Plan but implementation not yet verified	Must cite the document section
🔍 Inferred	Architectural interpretation derived from confirmed facts	Must name the specific confirmed facts it's built from, inline — not just assert the conclusion
🔮 Envisioned	Future direction, intentionally outside current scope	Roadmap/future-doc reference
⚠ Gap	Known inconsistency, undefined behaviour, or missing implementation already identified	Note whether it's a code gap or a documentation-drift gap (spec exists but was never reconciled into the reference docs) — these need different fixes
Bounding statement (methodology, stated once, applies everywhere): All ✅ Confirmed tags in this document are bounded by evidence pasted into this chat by Keshav — terminal output, migration file contents, commit messages, `openapi.json` dumps, click-through reports. There is no live repository access. Anything built in a Claude Code session since the last paste is invisible here and must not be assumed. Where a claim's freshness matters, the tag states the date/commit explicitly; treat anything older than the last full `git log` pull as "last known," not "current."
---
0.2 Google Stitch UI Governance
Google Stitch is the single authoritative source for all UI/UX design across VidhiDesk (Contracts, Litigation, RERA, Consulting, Administration). Implementation must never become the source of UI design.

Every UI screen must pass through this mandatory 6-step lifecycle before code is written:
1. **Step 1: Architecture Design:** Define business workflow, user journey, information architecture, and required components (No UI implementation).
2. **Step 2: Google Stitch Verification:** Audit whether an appropriate Google Stitch design already exists. Provide module, screen name, Stitch project ID, screen name/ID, availability status, confidence %, screenshot evidence link, gap analysis, and recommendation. A screen must NOT be marked "Verified Existing" or "Verified Partial" unless screenshot evidence has been reviewed.
3. **Step 2.5: Visual Design Review:** Open the actual Google Stitch screen. Conduct a visual inspection covering: Full-size screenshot, UX review, Missing elements, Incorrect elements, Design inconsistencies, Mobile considerations, Accessibility observations, Business workflow coverage, and Final Recommendation (`Approved without changes`, `Minor Stitch refinement required`, `Major redesign required`).
4. **Step 3: Design Generation & Refinement:** If a screen is Partial or requires refinement, produce an updated Google Stitch prompt, generate/refine the screen in Stitch, and re-review.
5. **Step 4: Design Approval:** Obtain explicit user signoff on the generated/verified Stitch design. Approved Stitch designs become the UI source of truth.
6. **Step 5: Implementation:** Only after approval, build React/Next.js components, wire backend APIs, connect business logic, and integrate AI services.

**Mandatory UI Tracker Lifecycle States:**
- `Verified Existing (Screenshot attached)`
- `Verified Partial (Screenshot attached)`
- `Visual Design Review Completed`
- `Newly Generated in Stitch`
- `Pending Schema Alignment`
- `Approved for Implementation`
- `Implemented`

No screen may move directly from "Designed" to "Implemented".
---
0.3 Official UI Signoff & Freeze State (5 August 2026)

**1. Design Frozen & Implemented (9 Subsystems/Screens):**
- Login (`/login`)
- Dashboard (`/dashboard`)
- Contracts Picker (`/contracts`)
- Contract Workspace (`/contracts/[matterId]`)
- Document Vault (`/documents`)
- Admin Dashboard (`/admin`)
- Template Review Admin (`/admin/templates`)
- Advocate Profile (`/profile`) — Status: `✅ Confirmed Implemented`. Dedicated backend endpoints (`GET /api/profile`, `PUT /api/profile`, `POST /api/profile/avatar`), migration DDL `api/migrations/0011_create_advocate_profiles.sql`, and 5-test regression suite (`api/tests/test_profile.py`).
- Litigation Core Data Foundation (`/matters/[id]`) — Status: `✅ Confirmed Implemented (Sprint 3.5.1 Increment 1)`. Migration DDL `api/migrations/0013_litigation_schema.sql`, Litigation Matter extensions, Parties CRUD, Facts & Exhibits Timeline, Hearing Docket, and 4-test regression suite (`api/tests/test_litigation.py`).
- Limitation Intelligence Engine (`/matters/[id]`) — Status: `✅ Confirmed Implemented (Sprint 3.5.2A)`. Rule-based Limitation Analysis service (`app/services/limitation.py`), statutory Articles (Art 19, 20, 54, 55, 58, 64, 65, 115, 116, 136), Section 5 condonation analysis, `POST /api/litigation/limitation-calculator`, and 5 unit/integration tests (`api/tests/test_limitation.py`).
- Forum & Jurisdiction Intelligence Services (`/matters/[id]`) — Status: `✅ Confirmed Implemented (Sprint 3.5.2B)`. Rule-based Forum Advisor service (`app/services/forum.py`), territorial jurisdiction (Sec 15-20 CPC 1908), state pecuniary thresholds (Delhi, Maharashtra, Karnataka, General), RERA Act 2016, Commercial Courts Act 2015, `POST /api/litigation/forum-advisor`, and 7 boundary & cross-state unit/integration tests (`api/tests/test_forum.py`).
- Evidence File Upload & AI Case Analysis (`/matters/[id]`) — Status: `✅ Confirmed Implemented (Sprint 3.5.3 — End-to-End Advocate Experience vertical slice)` [E28]. Real exhibit/document upload to Supabase Storage with graceful fallback (`POST /api/matters/{id}/evidence/upload`), migration `0014_litigation_case_analysis.sql`, and versioned AI Case Analysis generation (`app/services/case_analysis.py`, `POST`/`GET /api/matters/{id}/case-analysis`) combining the Matter Engine, RAG Retriever, Limitation Engine, Forum Advisor, LLM Gateway (new `case_analyst` task type), and Citation Verifier behind a deterministic/LLM trust boundary — see `docs/30_Implementation/ADR/ADR-011-ai-case-analysis-before-pleading.md`. New frontend tab and components (`litigation-case-analysis.tsx`, extended `litigation-fact-timeline.tsx`). 15 new backend regression tests (`test_case_analysis.py`: 9, `test_evidence_upload.py`: 6); 210/210 backend pytest tests, 0 lint errors, 13/13 frontend static pages passed. Explicitly NOT a pleading generator — see §8.1, which remains open.
**2. Architectural Intent Note (Simplified Advocate Profile Schema):**
- *Note:* Advocate Profile schema has been simplified in production to align with active database requirements (`id`, `user_id`, `full_name`, `designation`, `bar_number`, `primary_court`, `phone`, `office_address`, `avatar_url`, `created_at`, `updated_at`).

**3. Pending Stitch Refinement & Re-Review (2 Screens):**
- Cause List Calendar (`/calendar`) — Requires Indian court cause-list docket table (Item No., Bench, Stage of Hearing).
- Clause Review Detail (`/admin/templates/[key]`) — Requires legal boilerplate toggle switch and bulk-keep audit bar.

**4. Litigation Module Gating Rule:**
- Implementation of Litigation features is strictly gated. No Litigation implementation may begin until all Litigation Stitch designs complete the 6-step lifecycle and obtain user signoff.
---
1. Source Documents & Precedence
Doc	Date	Role	Status
`nitesh_recording.txt`	—	Raw requirements source (transcript)	📐 Reference only
`01_Scope_of_Work.md`	23 Jul 2026	Original Phase 1 scope, Litigation-first build order	📐 Superseded on build order (see §1.1)
`02_Technical_Requirements.md`	23 Jul 2026	Architecture, stack, data model, subsystems	📐 Authoritative for intended architecture — ⚠ the actual schema has since outgrown it (see §8.2)
`03_Implementation_Plan.md`	23 Jul 2026	Original sprint plan (S0–S7), Litigation → Contracts → Consulting → RERA	📐 Superseded by Project Plan's build order
`Project_Plan_Legal_AI_Assistant.md`	23 Jul 2026	Revised plan after IK API secured + no sample drafts available	📐 Authoritative for build order and phasing rationale
`UI_UX_Design_Notes.md`	3 Aug 2026	Design system (palette, type, layout skeleton, tone)	📐 Authoritative for design
`Stitch_Mockup_Plan.md`	3 Aug 2026	Screen-by-screen mockup list, sprint/session breakdown, priority order	📐 Authoritative for screen inventory — ⚠ actual build did not follow session boundaries literally (see §8.8)
`Navigation_and_Functional_Spec.md`	3 Aug 2026	Navigation map, link matrix, functional/validation spec per screen	📐 Authoritative for intended behavior/backend wiring
`LITIGATION_ARCHITECTURE.md`	3 Aug 2026	Sprint 3.5 Litigation Module architecture, database schemas, RAG pipeline, IK integration, screen specs	📐 Authoritative for Sprint 3.5 Litigation execution [E23]
1.1 Key reconciliation — Contracts-first rationale (corrected)
📐 Designed, per Project Plan §2: `01_SOW`/`03_Implementation_Plan` assumed Litigation ships first. The revised Project Plan flipped this to Contracts first.
🔍 Inferred (from: Project Plan §2 "Build order" text + the no-sample-drafts constraint documented in Project Plan §6): These are two separate decisions, previously conflated in an earlier draft of this document's understanding. (1) Contracts-first was a product-strategy decision — largest revenue portfolio, template-constrainable, near-zero citation-hallucination risk, and Litigation is precisely where Nitesh's own expertise makes him least dependent on the tool. (2) Clause-by-clause review exists as a separate response to a different problem — no gold-standard contract drafts existed to imitate, so legal correctness had to be captured incrementally at the clause level instead of via whole-document approval. Contracts wasn't chosen because clause review was invented; clause review was invented because Contracts (like everything else) had no sample corpus.
---
2. Product Recap (📐 Designed — SOW §1.1, unchanged across all docs)
Single dashboard, four module tiles:
Module	Core function	Primary risk	Build status
Litigation	Query → validate → provisions → draft pleading, with verified case citations	Citation hallucination	📐 Pleading generator designed in full [E23], not yet built — but ✅ Confirmed live end-to-end up to (not including) pleading drafting [E28]: matter creation, parties, facts/evidence incl. real file upload, limitation calculator, forum advisor, and versioned AI Case Analysis (Sprint 3.5.3 vertical slice) all wired and tested
Contracts	Pick 1 of 25–30 templates → intake → generate → amend by chat command	Legal correctness without gold-standard drafts	✅ Confirmed live — 10 of 10 Phase 1 templates live [E2][E8][E16][E20], 3-panel workspace live [E17], P0 Stabilization complete [E21][E22], canonical template_key normalization live [E25], Matter-Centric Loading Model live [E26], PROFILE-01 advocate profile isolation live [E27]
RERA / Real Estate	Property deeds (Transfer of Property Act family) + RERA complaint drafting + state filing walkthroughs	State-by-state rule drift	🔶 Backend implemented and unit-tested [E41]: reuses the existing Contracts template engine for deeds/complaints (one Sale Deed + one RERA Complaint template seeded, more deed types are the same pattern, not yet authored), new walkthrough state/procedure/progress API (migration 0019, not yet applied to production), RLS enforced. No frontend built. No curated state/procedure content seeded (deliberate — never fabricate legal/procedural content). See `docs/30_Implementation/RERA_BACKEND_INTEGRATION_CONTRACT.md`.
Consulting & Litigation Support	Facts in → applicable law, forum, remedy, limitation; strategy briefs for matters argued by other counsel	~80% reuse of Litigation engine	🔶 Backend implemented and unit-tested [E42]: `POST /api/consulting/analyze` + `GET /api/consulting/matters/{id}/analyses`, reuses Matter Engine/RAG/Citation Verifier/PII Masker/LLM Gateway exactly as designed. Migration 0020 not yet applied to production. No frontend built. Strategy-brief (litigation-support) half of the module not yet built. See `docs/30_Implementation/CONSULTING_BACKEND_INTEGRATION_CONTRACT.md`.
Non-negotiable across all four (📐 Designed, TRD §3.3): every case citation is either verified against the Indian Kanoon API and hyperlinked, or rendered as `⚠ UNVERIFIED`. Enforced in the renderer (no `ik_id` in DB → no hyperlink can render), not just in the prompt. Confirmed implemented in engine [E1]; active rendering on live citation-bearing screens will accompany Litigation/Consulting workspace launches.
🔍 Inferred addition, not in original SOW (from: Project Plan §1 cost framing + TRD stack table — Gemini/Groq/Ollama free tiers, Supabase free tier, Indian Kanoon as sole paid dependency): the effectively-zero-recurring-cost constraint is not a side detail — it explains the multi-provider LLM failover chain, the platform choice, and the deliberate exclusion of SCC Online/Manupatra. It functions as a primary architectural driver, not an isolated requirement line, and should be read as such throughout this document.
---
3. Design System Snapshot (📐 Designed — from `UI_UX_Design_Notes.md` + Stitch base context)
Feel: "Claude's calmness meets a barrister's chambers." Quiet authority, professional restraint. Explicitly not startup-flavored, not colorful, not gamified, not BigLaw-cold.
Reference points: Claude.ai, Notion, FT digital, Apple iBooks (restraint) · Bloomberg Terminal, WSJ app (professional gravity). Avoid: colorful Indian legaltech tiles/gradients/gamification, consumer SaaS chattiness, over-formal BigLaw sites.
Palette (exact hex — use everywhere, no deviation):
Role	Hex
Background	`#FBF9F5` (warm ivory)
Primary text	`#1A1A1A` (warm charcoal)
Primary accent	`#1E2A4A` (deep navy)
Critical actions	`#7A2A2A` (muted burgundy)
Success / verified	`#3D5A3D` (muted forest green)
Secondary text	`#6B6B6B` (warm gray)
Borders	`#E5E3DE` (warm gray)
Typography: IBM Plex Serif (long-form legal content, drafts) · IBM Plex Sans (UI). Body 15–16px, UI labels 14px, headings 20–24px.
Iconography: Monoline, single-color, 1.5px stroke, warm gray or navy only.
Layout skeleton (every screen): Global header (56px) · Global footer disclaimer · Left sidebar (persistent desktop / drawer mobile).
Copy tone: Formal, restrained. "Draft" not "Create." "Matter" not "Case." Indian date convention.
✅ Confirmed implemented across all 13 application routes [E17][E18][E19]: `LegalDocumentSheet` renders IBM Plex Serif 800px legal paper canvas; header shell features notification bell, settings, avatar, and mobile bottom navigation bar.
---
4. Phase (Project Plan) → Sprint (Stitch/Nav Spec) Mapping
Project Plan Phase	Weeks	Stitch/Nav Sprint equivalent	Status
Phase 0 — Scope freeze + template sourcing	3 wks	Sprint 1 (pre-tracker)	✅ Confirmed done [E1: commits `219a9aa`, `28e6e85`]
Phase 1 — Contracts MVP	8–10 wks	Sprint 2 + Sprint 3	✅ Confirmed COMPLETE [E2][E3][E8][E16][E17][E20][E21][E22][E24][E25][E26][E27] — 10 of 10 Phase 1 templates live, workspace 100% complete, all P0 backend & frontend stabilization complete, template key normalization complete [E25], Matter-centric loading model complete [E26], PROFILE-01 complete [E27]
Phase 2 — Litigation + citation engine	6–8 wks	Sprint 3.5 (Litigation Pleading & Case Law Workbench)	🔶 Partially built: Sprint 3.5.1 (Core Data Foundation), 3.5.2A (Limitation), 3.5.2B (Forum), and 3.5.3 (AI Case Analysis vertical slice, [E28]) all ✅ Confirmed. Pleading generation itself (§8.1) remains 📐 architecture designed [E23], not yet built.
Phase 3 — RERA + Consulting	6 wks	Sprint 4 (Consulting) + Sprint 5 (RERA)	🔶 Both backends implemented and unit-tested [E41][E42]; no frontend for either — tiles live on dashboard
Phase 4 — Productisation (post-launch)	ongoing	Sprint 6 (Hardening) + beyond	📐 Designed only, no build evidence
---
5. Master Sprint / Session / Screen Tracker
Status legend: all cells below carry a tag + evidence citation. Where a Stitch "session" boundary didn't match how the build actually proceeded (see §8.8), that's noted directly in the row.
Sprint 1 — Foundations (pre-tracker)
Screen	Route	Status
Login	`/login`	✅ Confirmed [E1: Sprint 0 commit `28e6e85`, Lex Scripta V2 design commit `822126d`]
Dashboard	`/dashboard`	✅ Confirmed [E3: commit `6c6bdc6`, Stitch V2 4-card module layout `d928c15`]
Contracts Template Picker (initial)	`/contracts`	✅ Confirmed live [E2: `web/src/app/contracts/page.tsx`, Stitch V2 layout `5fdf32f`]
---
Sprint 2 — Contracts Module Completion — ✅ Confirmed 100% built for UI & all 10 Phase 1 templates [E16][E17][E20][E21][E22][E24][E25][E26][E27]
Session (as planned)	Focus	Route(s)	API Endpoints	DB Tables	Status
1	Intake Form	`/contracts` & `/contracts/[matterId]`	`POST /api/matters` · `PATCH /api/matters/[id]` · `GET /api/state-rules`	`matters`, `templates`, `state_rules`, `draft_versions`	✅ Confirmed built [E2: `intake-form.tsx`; E8: E2E walkthrough; E17: Stitch V2 intake integration; E26: Matter-centric loading model]. Collapsible groups & live party title auto-generation active across all 10 templates.
2	Draft View	`/contracts/[matterId]`	`GET /api/drafts/[id]/download`, `.pdf` · `POST /api/matters/[id]/drafts`	`draft_versions`, `matters`, `draft_clause_fills`	✅ Confirmed built [E17: `LegalDocumentSheet` serif canvas, `ContractAiAssistant` right sidebar copilot, sticky formatting toolbar, status footer bar; E21: HTTP 504 timeout protection; E22: mobile AI slide-over sheet].
3	Clause Review Admin	`/admin/templates/[key]`	`POST /api/contracts/templates/{id}/clauses/{cid}/review` · `.../bulk-keep-boilerplate`	`templates`, `template_clauses` ✅, `clause_reviews` ✅	✅ Confirmed built and exercised [E5: migration 0007; E11: review-state sweep; E20: 10 templates supported]. Admin template index ([`/admin/templates`](file:///home/keysh/vidhidesk/web/src/app/admin/templates/page.tsx)) live.
4	Header Shell & Controls	Applies globally	Supabase Auth metadata	`auth.users`	✅ Confirmed built [E17][E18][E27]: Header notifications icon, settings gear, advocate avatar frame, and Advocate Profile page ([`/profile`](file:///home/keysh/vidhidesk/web/src/app/profile/page.tsx)) with photo upload, Supabase user_metadata server-first loading [E27], onboarding profile alert banner, and password reset options.
5	Mobile Responsive Pass	`/contracts`, `/dashboard`, `/profile`	`GET /api/templates`	`templates`, `matters`	✅ Confirmed built [E17][E18][E22]: Mobile bottom nav bar (`Home`, `Documents`, `Research / Drafts`, `Calendar`), left drawer navigation, responsive document sheet layout, and mobile floating AI assistant trigger button & slide-over sheet.
Templates actually shipped (✅ Confirmed [E2][E9][E16][E20][E25], 10 of 10 Phase 1 templates live in production):
1. NDA — 12 clauses (`nda`)
2. Service Agreement — 11 clauses (`service-agreement`) [E25]
3. Consultancy — 10 clauses (`consultancy`)
4. MoU — 9 clauses (`mou`)
5. Employment — 12 clauses (`employment`)
6. Leave & Licence Agreement — 7 clauses (`leave-licence`) [E20]
7. Lease Deed — 8 clauses (`lease-deed`) [E20]
8. Agreement to Sell — 8 clauses (`agreement-to-sell`) [E20]
9. Joint Venture Agreement — 8 clauses (`joint-venture`) [E20]
10. Software Development & Maintenance Agreement — 9 clauses (`software-dev`) [E20]
Sprint 2 exit gate (✅ Confirmed met for all 10 templates [E17][E20][E21][E22][E24][E25][E26][E27]): all 10 templates generate complete drafts from intake; state selection provides stamp-duty notes; amendment commands version without loss; drafts export .docx and .pdf with 15s timeout protection; all 10 templates render cleanly on Contracts Workspace grid; profile page isolates advocate data per user without hardcoded defaults [E27].
---
Sprint 3 — Refinements, Navigation & Admin Improvements
Session	Focus	Route(s)	Status
S3.1	Admin templates index (all-templates review status)	`/admin/templates`	✅ Confirmed built [E3: commit `6c6bdc6` — `web/src/app/admin/templates/page.tsx` created].
S3.2	Advocate Profile & Settings Page	`/profile`	✅ Confirmed built [E18: commit `c1c118d`, `6eec6a3`; E27: PROFILE-01 removed demo strings, server-first user_metadata persistence, onboarding banner].
S3.3	Cause List Calendar Page	`/calendar`	✅ Confirmed built [E19: commit `c1c118d` — litigation hearing dockets schedule].
S3.4	Advocate Document Vault	`/documents`	✅ Confirmed built [E19: commit `c1c118d` — versioned `.docx` draft file store].
S3.5	Template Expansion Helper Refactoring	`api/scripts/template_seed_utils.py`	✅ Confirmed built [E20: centralized `seed_template_pipeline` to eliminate seed script code duplication].
S3.6	Phase A P0 Backend Stabilization Fixes	`SEC-01`, `PERF-01`	✅ Confirmed built [E21: XML prompt injection boundary delimiters across all LLM entry points + LibreOffice 15s subprocess timeout protection with HTTP 504 error response].
S3.7	Phase 1 Frontend P0 Stabilization Fixes	`MOB-01`, `UX-01`, `UX-02`	✅ Confirmed built [E22: Mobile AI Assistant floating trigger & slide-over sheet + real template category filter checkboxes with combined search & empty state + Lex Scripta error recovery card with retry button].
S3.8	Sprint 3.5 Litigation Architecture Design	`docs/LITIGATION_ARCHITECTURE.md`	📐 Designed in full [E23: complete 15-section architecture specification, database additions, RAG pipeline, IK integration, security model, and component reuse breakdown].
S3.9	LLM Gateway Intra-Provider Model Pool Failover	`api/app/services/llm_gateway.py`	✅ Confirmed built [E24: intra-provider model pools with pinned versions; Gemini 4 models, Groq 5 models, SambaNova 1 model, Cerebras 3 models; per-model transient retry + position logging].
S3.10	Canonical Template Key Normalization	`service-agreement`	✅ Confirmed built [E25: standardized all 10 templates to kebab-case across DB, seed scripts, schemas, frontend filter, migration 0009, and regression tests].
S3.11	Matter-Centric Loading Model Release	`/contracts/{matterId}`	✅ Confirmed built [E26: added template_id column to matters table (migration 0009/0010), updated MatterCreate/Out schemas, standardized get_template for UUID & slug lookup, clean matter-centric routing without requiring ?template=].
S3.12	PROFILE-01 Advocate Profile Isolation Release	`/profile`	✅ Confirmed built [E27: removed hardcoded advocate demo values, server-first user_metadata loading, onboarding alert banner for incomplete profiles, regression tests].
S3.13	Cross-template shared-clause detection banner	`/admin/templates/[key]` (enhancement)	📐 Requested in Batch 4.5 message [E13], not confirmed built
S3.14	Content-hash mismatch warning (clause changed since review)	`/admin/templates/[key]` (enhancement)	⚠ Gap remains — not built as far as evidence shows
---
Sprint 4 — Consulting & Litigation Support Module
Session	Focus	Route(s)	Status
Backend [E42]	🔶 Implemented and unit-tested (19/19 `test_consulting.py`; full suite regression — see PR evidence). `api/app/routers/consulting.py` + `api/app/services/consulting.py` (`POST /api/consulting/analyze`, `GET /api/consulting/matters/{id}/analyses`), new `consulting_analyses` table (migration `0020_consulting_backend.sql`, not yet applied to production), RLS enforced. Reuses the existing Matter Engine (module='consulting'), RAG Retriever, Citation Verifier, PII Masker, LLM Gateway (`consulting_analyst` task type, extended to a structured JSON contract), and — when the caller supplies them — the deterministic Limitation Calculator / Forum Advisor. No new matter entity, no new AI provider, no new citation verifier, no curated legal content seeded. No frontend built. See `docs/30_Implementation/CONSULTING_BACKEND_INTEGRATION_CONTRACT.md` for the full endpoint contract handed to the frontend/UI agent.
S4.1–S4.4	Consulting landing, analysis result, mobile, citation card	`/consulting`, `/consulting/[matterId]`	⬜ Designed only — tile live on dashboard
---
Sprint 5 — RERA / Real Estate Module
Backend [E41]: 🔶 Implemented and unit-tested (20/20 `test_rera.py`, full suite 321/321). `api/app/routers/rera.py` + `api/app/services/rera.py` (state/procedure/walkthrough-step/progress endpoints, new), Sale Deed + RERA Complaint templates seeded via the existing Contracts template engine (zero new drafting code). Migration `0019_rera_backend.sql` authored, not yet applied to production. No frontend UI (`web/src/app/rera/...` does not exist yet) — out of this session's explicit scope. See `docs/30_Implementation/RERA_BACKEND_INTEGRATION_CONTRACT.md` for the full endpoint contract handed to the frontend/UI agent.
All other sessions (S5.1–S5.6, UI/Stitch design work): ⬜ Designed only — tile live on dashboard.
---
Sprint 6 — Hardening & Admin
All sessions (S6.1–S6.2): ⬜ Designed only.
---
6. Cross-Cutting Backend Subsystems
Subsystem	Status
LLM Gateway (Gemini → Groq → SambaNova → Cerebras)	✅ Confirmed built, Sprint 0 [E1: commit `28e6e85`], 4-tier provider failover, intra-provider model pools [E24], SEC-01 prompt isolation [E21]
Conversation history across turns	✅ Confirmed built [E12]: bounded 6-message window using stored `masked_prompt`
PII Masker & Unmasker	✅ Confirmed built [E12: per-string masking, statute-abbreviation allowlist, `pii_masks` DB table]
RAG Retriever (statute knowledge base)	✅ Confirmed built, Sprint 1 [E1: commit `0b165e8` — statute RAG + hybrid retrieval]; 📐 Extended for Litigation [E23]
Citation Verifier (Indian Kanoon API) + renderer gate	✅ Confirmed built, Sprint 1 [E1: commit `0b165e8` — `citations` verification gate]; 📐 Extended for Litigation [E23]
Golden test harness	✅ Confirmed built [E1: 5 golden test fact patterns in `docs/golden_tests.json`]
Template Engine (Jinja2 → python-docx)	✅ Confirmed built & extended [E2][E6][E20][E25]: `applicable_condition` JSONB (migration 0008), clause renumbering, `an_or_a` grammar filter, 10 templates supported with kebab-case key normalization [E25]
LibreOffice PDF Timeout Protection	✅ Confirmed built [E21: 15s timeout cap, `PdfConversionTimeout`, HTTP 504 response]
Shared Seed Pipeline Utilities	✅ Confirmed built [E20: `api/scripts/template_seed_utils.py`]
Debounced Title Auto-Generator	✅ Confirmed built [E17: `web/src/lib/matter-title.ts`, `PATCH /api/matters/[id]`]
Formatted Legal Document Canvas	✅ Confirmed built [E17: `web/src/components/legal-document-sheet.tsx` (IBM Plex Serif 800px sheet)]
Persistent AI Legal Copilot Sidebar	✅ Confirmed built [E17: `web/src/components/contract-ai-assistant.tsx` (Risk analysis, suggestions, citations); E22: mobile slide-over sheet]
Template Category Checkbox Filter & Search	✅ Confirmed built [E22: `web/src/app/contracts/page.tsx`; E25: normalized key matching rendering 10/10 templates]
Matter-Centric Loading Model & Clean Routing	✅ Confirmed built [E26: stored template_id on matters table, clean `/contracts/{matterId}` URLs, robust UUID/slug lookup]
Advocate Profile Data Isolation (PROFILE-01)	✅ Confirmed built [E27: server-first user_metadata loading, onboarding alert banner, removed hardcoded advocate demo values]
Evidence File Upload (Supabase Storage)	✅ Confirmed built [E28: `POST /api/matters/{id}/evidence/upload`, non-fatal storage-failure fallback, 10MB/type limits]
AI Case Analysis Engine	✅ Confirmed built [E28: `app/services/case_analysis.py`, versioned per-matter, deterministic/LLM split, statute-grounding cross-check, Citation Verifier reuse for proposed precedents]
---
7. Current Status Snapshot
7.1 Confirmed build log (✅ — chronological, per pasted evidence)
Sprint 0 [E1, commit `28e6e85`]: foundations, IK API spike, LLM Gateway, PII masker, "Hello matter" exit criterion met.
Sprint 1 [E1, commit `0b165e8`]: statute RAG + citation verifier + renderer gate + hybrid retrieval + keyword search + golden harness.
Sprint 2 build [E2, commit `987a1da`]: 5 Contracts templates (NDA, Service Agreement, Consultancy, MoU, Employment), template engine with clause-review workflow, bulk-keep-boilerplate action, re-seed guard, migrations 0007–0008, deployment infra, CORS env-var support, PII masker hardening.
Sprint 2.5 [E3, commit `6c6bdc6`]: dashboard rewrite, new `/admin/templates` index page, navigation shell.
Dashboard & Navigation V2 [E18, E19, commits `85f0230`, `d928c15`, `dddf74b`, `35179f9`, `c1c118d`, `6eec6a3`]: 4-card module layout, left navigation drawer, top-right header controls (Notifications, Settings, Avatar), mobile bottom nav bar, Advocate Profile page (`/profile`) with Base64 photo upload & Supabase password reset, Cause List Calendar (`/calendar`), and Document Vault (`/documents`).
Stitch V2 Contracts Workspace Release [E17, commit `5fdf32f`]: 3-panel workspace layout with 280px left Matter Navigator, `LegalDocumentSheet` (800px IBM Plex Serif paper canvas), `ContractAiAssistant` (320px persistent right sidebar AI copilot), sticky rich text formatting toolbar, and bottom status bar (*Auto Save Active*, *Version*, *Word Count*, *AI Status: Ready*, *Citation Verified*).
Phase 1 Contract Template Expansion [E20]: 5 new production contract templates built and seeded into Supabase (`Leave and Licence Agreement`, `Lease Deed`, `Agreement to Sell`, `Joint Venture Agreement`, `Software Development Agreement`), bringing total template inventory to 10 of 10 Phase 1 target templates. Extracted `api/scripts/template_seed_utils.py` shared pipeline. All 23 backend pytest cases passed cleanly.
Phase A P0 Backend Stabilization Fixes [E21]: Implemented `SEC-01` (XML-style prompt boundary isolation `<user_instruction>` / `<user_amendment>` across all LLM entry points) and `PERF-01` (LibreOffice PDF conversion 15s subprocess timeout cap with `PdfConversionTimeout` and HTTP 504 error response). Added 3 unit tests. All 165 backend pytest tests passed cleanly.
Phase 1 Frontend P0 Stabilization Fixes [E22]: Implemented `MOB-01` (Mobile AI Assistant floating trigger button & slide-over sheet), `UX-01` (Template category filter checkboxes with real filtering, combined search, and structured empty state), and `UX-02` (Lex Scripta structured error recovery cards with actionable retry buttons). Verified `npm run lint` (0 errors) and `npm run build` (13/13 static routes).
Sprint 3.5 Litigation Architecture Design [E23]: Delivered `docs/LITIGATION_ARCHITECTURE.md` comprehensive architecture specification for Sprint 3.5 Litigation Pleading & Case Law Workbench.
LLM Gateway Intra-Provider Model Pool Failover [E24]: Refactored `api/app/services/llm_gateway.py` to try an ordered pool of pinned models within each provider tier before escalating to the next provider. Configured 4 Gemini models, 5 Groq models, 1 SambaNova model, and 3 Cerebras models. All 169 backend pytest tests passed cleanly.
Canonical Template Key Normalization [E25]: Standardized all template keys to kebab-case (`service-agreement`). Created migration `0009_normalize_template_keys.sql`, updated Supabase database, seed script, JSON schema, and frontend category filter logic (`web/src/app/contracts/page.tsx`). Added 2 backend regression tests. All 10/10 templates render cleanly on Contracts Workspace grid.
Matter-Centric Loading Model Release [E26]: Implemented permanent template association on matters table (`0010_add_template_id_to_matters.sql`), clean `/contracts/{matterId}` URLs, standardized `get_template()` for UUID & slug lookup, and updated all frontend navigation links. Added regression tests. 173/173 backend pytest tests and 13/13 frontend static pages passed.
PROFILE-01 Advocate Profile Isolation Release [E27]: Removed all hardcoded advocate demo values (`Adv. Nitesh Sharma`, `D/1042/2018`, etc.), implemented server-first loading from Supabase Auth `user_metadata`, added a friendly onboarding alert banner for incomplete profiles, and updated `AuthedShell` header avatar synchronization. Added regression tests. 174/174 backend pytest tests and 13/13 frontend static pages passed.
Deployment [E15]: backend live on Render (`vidhidesk.onrender.com`), frontend live on Vercel; confirmed working end-to-end.
Sprint 3.5.3 — End-to-End Advocate Experience Vertical Slice [E28]: Delivered the first complete Litigation workflow: create matter → add parties → record facts → upload evidence (real files, Supabase Storage) → run Limitation Analysis → run Forum Analysis → generate versioned AI Case Analysis (matter summary, chronological facts, missing information, applicable statutes, possible causes of action with grounded/ungrounded statute flagging, jurisdiction summary, limitation summary, potential risks, evidence gaps, recommended next steps, and Citation-Verifier-checked possible precedents) → structured review workspace. Explicitly stops short of pleading generation (ADR-011). New migration `0014_litigation_case_analysis.sql`, new service `app/services/case_analysis.py`, new `case_analyst` LLM Gateway task type, new frontend tab and components. 15 new backend tests; 210/210 backend pytest tests, 0 ESLint errors, `npm run build` 13/13 static routes passed. Live browser click-through with a real Supabase/LLM backend was not performed in this environment (no `api/.env` credentials provisioned here) — verified instead via full test suite, typecheck, lint, production build, and a local server boot confirming all new routes register correctly against `/openapi.json`.
7.2 Not started / no evidence
Litigation pleading generator workspace implementation (architecture designed [E23], now sequenced to build on top of the Sprint 3.5.3 AI Case Analysis per ADR-011 rather than directly on facts intake), RERA filing wizard, Consulting advisory brief generator.
---
8. Flagged Gaps
8.1 ⚠ Litigation pleading generator pending implementation — Architecture designed [E23]
Cause List Calendar page ([`/calendar`](file:///home/keysh/vidhidesk/web/src/app/calendar/page.tsx)), RAG retrieval backend, and the new AI Case Analysis step [E28] all exist and are confirmed built. Full pleading *drafting* workspace architecture is specified in `docs/LITIGATION_ARCHITECTURE.md` [E23], and per ADR-011 should now build on top of the Case Analysis rather than directly on facts intake; still awaiting user signoff before code implementation.
8.2 `template_clauses` / `clause_reviews` — ✅ RESOLVED IN CODE, gap was documentation drift only
Defined and built via migration 0007 [E5] — `template_clauses`, `clause_reviews`, `draft_clause_fills`, plus `templates.template_key` and `templates.review_status`.
8.3 Sprint 3 admin features — partially requested, not confirmed built
Cross-template shared-clause detection requested in Batch 4.5 message [E13] but no build confirmation exists. Content-hash mismatch warning has no build evidence.
8.4 Template Inventory Completed — ✅ 10 of 10 Phase 1 templates live in production [E16][E20][E25]
All 10 Phase 1 templates seeded, normalized, and verified (`nda`, `service-agreement`, `consultancy`, `mou`, `employment`, `leave-licence`, `lease-deed`, `agreement-to-sell`, `joint-venture`, `software-dev`).
8.5 Technical Debt & Stabilization — ✅ ALL ISSUES RESOLVED [E21][E22][E24][E25][E26][E27]
Backend `SEC-01`, `PERF-01`, LLM Gateway intra-provider model pool failover [E24], canonical template key normalization [E25], Matter-Centric Loading Model [E26], and PROFILE-01 advocate profile isolation [E27] are fully implemented and verified.
---
9. Open Decisions
Nitesh's design involvement: 📐 confirmed zero involvement in UI decisions; his review is legal-content only.
Litigation sequencing proposal: Execute Sprint 3.5 for Litigation Pleading & Case Law Workbench following the specification in `docs/LITIGATION_ARCHITECTURE.md` [E23].
**Pleading generation go/no-go — pending Nitesh's acceptance testing:** the Sprint 3.5.3 AI Case Analysis vertical slice [E28] must pass a structured validation round against `docs/30_Implementation/Acceptance_Testing/Sprint_3.5.3_Acceptance_Testing_Guide.md` (26 scenarios, plus its `Product_Validation_Report_Template.md`) before any pleading-generation work (§8.1) begins, per that guide's §5 gate. The guide itself, authored 6 August 2026, identified four code-verified findings while it was being written: two confirmed defects — **TICKET-5** (off-by-one-day error in Article 115/116 limitation computation) and **TICKET-6** (forum-recommendation-ordering defect for Commercial Disputes meeting the Commercial Courts Act threshold) — and two scope/design gaps still open — **TICKET-7** (Uttar Pradesh/Bihar missing from the Forum Advisor's state table) and **TICKET-8** (AI Case Analysis has no path from `litigation_hearings` into the LLM prompt). **TICKET-5 and TICKET-6 were fixed the same day, before any human validation round ran** — `int()` truncation replaced with `round()` in `limitation.py`'s day-based branch, and `forum.py`'s Commercial Dispute ordering extended to match the RERA branch's already-correct append-not-insert pattern — both re-verified against the exact scenarios that found them and locked in with new regression tests (214/214 backend tests passing). See `docs/30_Implementation/Backlog.md` for the full fix record. TICKET-7/TICKET-8 remain open and documented per the guide's "document-and-proceed-with-caution" tier — they do not block starting the first validation round. **First validation round executed [E30], result: Hold.** Partial by environment necessity (no live LLM/Supabase credentials this session) — the deterministic Limitation/Forum layer was live-executed and matched expectations 100% of the time, but the LLM-synthesized AI Case Analysis layer, which pleading generation would actually be built on top of, remains completely unvalidated. See `docs/40_Validation/Go_No_Go_Decision.md`. Pleading-generation work remains gated pending a follow-up round (automated with real credentials, or manual by Nitesh) that actually exercises the AI-dependent layer. **Runtime infrastructure check [E31], result: Hold (reason updated).** Live credentials exist and work (Gemini/Groq/SambaNova/Indian Kanoon/Supabase DB all confirmed with real calls) — the E30 "no credentials" framing was itself incomplete (checked the wrong `.env` location). The real, current blocker: the Litigation migrations (0013/0014) were never applied to the production Supabase project, and no storage buckets are provisioned — TICKET-9/10/11. Once those are applied (a deployment action, not a code fix), the actual 26-scenario runtime validation this gate requires becomes executable. **Verification framework built [E32], result: Hold (unchanged, now re-runnable).** `python api/scripts/verify_project.py` is now the one-command way to re-check this gate's infrastructure preconditions — run it after applying TICKET-9/10 to confirm `Validation Ready: YES` before starting the next full validation round. Also added TICKET-12 (migration idempotency) and corrected a false documentation claim about migration numbering.
---
10. Handover Instructions
Read in this order: this tracker (§0.1 first) → source docs (§1) → `docs/LITIGATION_ARCHITECTURE.md` (§15) → the current codebase directly.
Check `/admin/templates` directly to see real clause-review progress across all 10 templates.
Every screen must: use the exact palette in §3, carry the advocate-review disclaimer, route citations through the Citation Verifier.
When updating this tracker: don't upgrade a tag from 📐/🔍 to ✅ without a fresh evidence citation added to §11.
---
11. Evidence Log
ID	Evidence	Date/Commit
E1	`git log --oneline -10` pasted by Keshav — commits through Sprint 0/1, `28e6e85` through `f55c9bb`	Prior to 3 Aug 2026 chat
E2	Commit `987a1da` — "Sprint 2: Contracts module — 5 templates..." — 43 files changed, 9,329 insertions	Pasted 3 Aug 2026
E3	"Sprint 2.5: dashboard + admin templates index + nav link" commit `6c6bdc6`	Pasted 3 Aug 2026
E4	`openapi.json` grep showing 14 routes post-redeploy	Pasted 3 Aug 2026
E5	Migration `0007_contracts_clause_review.sql` verification — `template_clauses`, `clause_reviews`, `draft_clause_fills` tables confirmed present	Pasted 3 Aug 2026
E6	Migration `0008_generic_clause_conditions_and_numbering.sql` — `applicable_condition` JSONB column, `heading` column	Pasted 3 Aug 2026
E7	NDA seed script verification queries — template row, 11 clauses	Pasted 3 Aug 2026
E8	Service Agreement live E2E walkthrough	Pasted 3 Aug 2026
E9	Consultancy + MoU click-through signoff messages	Pasted 3 Aug 2026
E10	Employment batch field/clause plan	3 Aug 2026
E11	Data-integrity sweep — re-seed review-state regression found and resolved	Pasted 3 Aug 2026
E12	Conversation-history bug diagnosis + fix — bounded 6-message window, `masked_prompt` reuse	Pasted 3 Aug 2026
E13	"Batch 4.5" message — bulk-keep-boilerplate + cross-template shared-clause detection requested	3 Aug 2026
E14	Batches 5–7 (Leave & Licence, Lease Deed, Agreement to Sell, Joint Venture, Software Development) sequencing approved in principle	3 Aug 2026
E15	Render (`vidhidesk.onrender.com`) and Vercel deployment confirmed live	Pasted 3 Aug 2026
E16	Contracts Template Inventory Audit — `ls templates/contracts/` & `find api/scripts/` confirmed exactly 5 templates exist (`NDA`, `Service Agreement`, `Consultancy`, `MoU`, `Employment`). Batches 5–7 confirmed unbuilt.	Pasted 3 Aug 2026
E17	Stitch V2 Contracts Workspace Release — `git commit 5fdf32f` (`LegalDocumentSheet`, `ContractAiAssistant`, 280px left Matter Navigator, sticky rich formatting toolbar, status bar, `PATCH /api/matters/[id]`). Verified `npm run lint` (0 errors) and `npm run build` (13/13 static routes).	Pasted 3 Aug 2026
E18	Advocate Profile & Settings Page Release — `git commit c1c118d`, `6eec6a3` (`web/src/app/profile/page.tsx` with photo upload, Supabase metadata sync, direct password update, and email password reset link).	Pasted 3 Aug 2026
E19	Cause List Calendar & Document Vault Pages Release — `git commit c1c118d` (`web/src/app/calendar/page.tsx` litigation hearing dockets schedule, `web/src/app/documents/page.tsx` versioned `.docx` draft file store).	Pasted 3 Aug 2026
E20	Phase 1 Contract Template Expansion (Batches 5–7) — 5 new templates implemented and seeded into Supabase (`Leave and Licence Agreement`, `Lease Deed`, `Agreement to Sell`, `Joint Venture Agreement`, `Software Development Agreement`), bringing total template inventory to 10 of 10 Phase 1 target templates. Extracted `api/scripts/template_seed_utils.py` shared pipeline. Verified 23/23 backend pytest cases passed cleanly.	Pasted 3 Aug 2026
E21	Phase A P0 Backend Stabilization Release — Implemented `SEC-01` (XML-style prompt boundary isolation `<user_instruction>` / `<user_amendment>` across all LLM entry points) and `PERF-01` (LibreOffice PDF conversion 15s subprocess timeout cap with `PdfConversionTimeout` and HTTP 504 error response). Added 3 unit tests. Verified 165/165 backend pytest tests passed cleanly.	Pasted 3 Aug 2026
E22	Phase 1 Frontend P0 Stabilization Release — Implemented `MOB-01` (Mobile AI Assistant floating trigger button & slide-over sheet), `UX-01` (Template category filter checkboxes with real filtering, combined search, and structured empty state), and `UX-02` (Lex Scripta structured error recovery cards with actionable retry buttons). Verified `npm run lint` (0 errors) and `npm run build` (13/13 static routes).	Pasted 3 Aug 2026
E23	Sprint 3.5 Litigation Architecture Specification — Authored `docs/LITIGATION_ARCHITECTURE.md` comprehensive architecture document covering 15 structural domains, database additions, RAG pipeline, Indian Kanoon API integration, security model, and component reuse analysis.	Pasted 3 Aug 2026
E24	LLM Gateway Intra-Provider Model Pool Failover Release — Refactored `api/app/services/llm_gateway.py` to support intra-provider model pools with pinned versions across Gemini (4 models), Groq (5 models), SambaNova (1 model), and Cerebras (3 models). Removed unused `rate_limited` attribute. Added 4 unit tests. Verified 169/169 backend pytest tests passed cleanly.	Pasted 3 Aug 2026
E25	Canonical Template Key Normalization Release — Standardized all 10 contract templates to kebab-case template keys (`service-agreement`). Created migration `0009_normalize_template_keys.sql`, updated Supabase database, seed script, JSON schema, and frontend category filter logic (`web/src/app/contracts/page.tsx`). Added 2 backend regression tests. All 10/10 templates render cleanly on Contracts Workspace grid.	Pasted 3 Aug 2026
E26	Matter-Centric Loading Model Release — Added `template_id` column to `matters` table (`0010_add_template_id_to_matters.sql`), updated `MatterCreate`/`MatterOut` schemas, standardized `get_template()` for UUID & slug lookup, and updated all frontend navigation links (`/contracts/{matterId}`). Added regression tests. 173/173 backend pytest tests and 13/13 frontend static pages passed.	Pasted 3 Aug 2026
E27	PROFILE-01 Advocate Profile Isolation Release — Removed all hardcoded advocate demo values (`Adv. Nitesh Sharma`, `D/1042/2018`, etc.), implemented server-first loading from Supabase Auth `user_metadata`, added a friendly onboarding alert banner for incomplete profiles, and updated `AuthedShell` header avatar synchronization. Added regression tests. 174/174 backend pytest tests and 13/13 frontend static pages passed.	Pasted 3 Aug 2026
E28	Sprint 3.5.3 End-to-End Advocate Experience Vertical Slice — Migration `0014_litigation_case_analysis.sql` (evidence file columns + `litigation_case_analyses` table with RLS); `app/services/case_analysis.py` (deterministic/LLM-split generation, statute grounding cross-check, Citation Verifier reuse); new `case_analyst` LLM Gateway task type; `POST /api/matters/{id}/evidence/upload`; `POST`/`GET /api/matters/{id}/case-analysis`; frontend `litigation-case-analysis.tsx` + extended `litigation-fact-timeline.tsx` + new matter workspace tab. 15 new backend tests (`test_case_analysis.py`: 9, `test_evidence_upload.py`: 6); 210/210 total backend pytest tests passed, 0 ESLint errors, `npm run build` 13/13 static routes, local `uvicorn` boot + `/openapi.json` route verification confirming all new routes registered correctly. No live Supabase/LLM credentials available in this environment for a full browser click-through — stated as a gap, not claimed as tested.	This session, 6 August 2026
E29	Sprint 3.5.3 Acceptance Testing Guide (26 scenarios) authored and published at `docs/30_Implementation/Acceptance_Testing/`, alongside a `Product_Validation_Report_Template.md`. Authoring the guide involved directly executing `calculate_limitation()` and `determine_forum()` against every scenario's stated inputs rather than relying on hand arithmetic, which surfaced two real defects — TICKET-5 (Article 115/116 limitation expiry computed one day short via `int()` truncation of a year-fraction) and TICKET-6 (Forum Advisor recommending the general civil court over the Commercial Court for qualifying Commercial Disputes) — plus two open design/scope gaps, TICKET-7 (UP/Bihar missing from the Forum Advisor's state table) and TICKET-8 (AI Case Analysis has no path from `litigation_hearings` into the LLM prompt). At the user's explicit request, TICKET-5 and TICKET-6 were fixed the same session, before any human validation round: `limitation.py`'s day-based branch changed from `int(years * 365)` to `round(years * 365)`; `forum.py`'s Commercial Dispute branch extended to append rather than displace the Commercial Court option, mirroring the RERA branch's already-correct pattern. Both fixes re-verified against the original scenarios (APP-01/APP-02/APP-03; COM-01/COM-02/COM-03/IA-03) and locked in with 4 new regression tests. The acceptance guide itself was updated in place to reflect the corrected expected values, with each affected scenario retaining a note on the original defect and its fix, since finding-and-closing a defect before the first validation round is itself a result worth keeping on record. TICKET-5 through TICKET-8 triaged into `docs/30_Implementation/Backlog.md` (TICKET-5/6 marked shipped, TICKET-7/8 remain open). 214/214 backend pytest tests passing.	This session, 6 August 2026
E30	Sprint 3.5.5 Product Validation round executed and published at `docs/40_Validation/`. **Partial by environment necessity**: this session has no live LLM provider keys, Supabase service key, or Indian Kanoon token, so the AI Case Analysis pipeline, evidence upload, citation verification, and full browser workflow could not be executed for any of the 26 scenarios — no output was fabricated to fill that gap. What was executed for real: `calculate_limitation()` and `determine_forum()` (pure, dependency-free functions) run directly against every scenario's documented inputs, plus the deterministic sub-layer of `case_analysis.py` (chronological sort, rule-based evidence-gap/missing-info seeding). Result: 100% match on all 46 real Limitation/Forum calls against the acceptance guide's expected values, live-reconfirming the E29 TICKET-5/TICKET-6 fixes across every state and category the guide designed to test them. One finding: the guide's own COM-04 expected-forum text is stale (predates the TICKET-6 fix; the system is actually correct, the document isn't) — logged, not corrected, per this sprint's "do not fix defects during validation" rule. TICKET-7/TICKET-8 re-confirmed where testable, left untouched as instructed. Zero new product defects. Deliverables: `Product_Validation_Report_2026-08-06.md`, `Validation_Summary.md`, `Metrics_Dashboard.md`, `Defect_Log.md`, `Recommendations.md`, `Go_No_Go_Decision.md`. Decision recorded: **Hold** — the deterministic layer is now live-verified, but the LLM-synthesized layer pleading generation would actually depend on remains completely unvalidated; see `Go_No_Go_Decision.md`.	This session, 6 August 2026
E31	Sprint 3.5.5A Runtime Infrastructure Validation — corrected a real error from E30: the prior round checked `api/.env` only and concluded no live credentials existed; `app/config.py` actually loads from the monorepo-root `.env`, which exists and contains real, working credentials for all 8 configured services. Verified each with a real, live call: Gemini ✅ (1246ms), Groq ✅ (271ms), SambaNova ✅ (2582ms), Cerebras ❌ (HTTP 401 invalid key — TICKET-11, no practical impact, fourth-tier fallback only), Indian Kanoon API ✅ (7812ms, 10 results), Supabase DB ✅ (1317ms, real authenticated query against `matters`, 71 existing rows). Then found the actual blocker: the real production Supabase project (`pgwemjswxdlnshrfoggj`) has never had migrations `0013_litigation_schema.sql` or `0014_litigation_case_analysis.sql` applied — `litigation_parties`/`litigation_facts_evidence`/`litigation_hearings`/`litigation_case_analyses` all return `PGRST205` table-not-found, independently re-confirmed via direct PostgREST OpenAPI schema introspection (13 tables total, zero litigation-related). Separately, zero Supabase Storage buckets are provisioned (`evidence`, `avatars` both missing) — degrades gracefully rather than crashing, per existing fallback code, but blocks genuine Evidence Upload testing. This makes the entire documented Litigation workflow unexecutable against production regardless of LLM health, since every scenario requires at least one `litigation_parties` row before `case_analysis.py`'s own precondition check will proceed. Per Sprint 3.5.5A's explicit "if a dependency is missing, stop and report it" rule, Phases 2–6 (the actual 26-scenario execution, AI quality scoring, performance/cost metrics, defect triage) were not attempted — no output was fabricated to fill the gap. Filed as TICKET-9 (Critical, migrations), TICKET-10 (Major, storage), TICKET-11 (Minor, Cerebras key) in `docs/30_Implementation/Backlog.md`. No application code, migrations, or infrastructure modified this session. Recommendation: **Hold**, unchanged from E30 but now for a precise, low-effort-to-fix reason rather than "no credentials." See `docs/40_Validation/Runtime_Validation_Report_2026-08-06.md`.	This session, 6 August 2026
E32	Sprint 3.5.5B Engineering Hardening & Verification Framework — built a reusable, re-runnable infrastructure verification framework (`api/scripts/verify_environment.py`, `verify_database.py`, `verify_storage.py`, `verify_llm_providers.py`, `verify_migrations.py`, `verify_runtime.py`, orchestrated by `verify_project.py`) replacing the one-off manual checks E31 did by hand. Every check is real — no mocks, no estimates — and reports SKIP with a stated reason (not a fabricated PASS) where a check genuinely can't be performed (e.g. named index/constraint existence needs a direct Postgres connection this project doesn't have configured). Full real run captured verbatim in `docs/40_Validation/Health_Report_Raw_Output_2026-08-06.txt`: Environment PASS, Migrations WARN, Database FAIL (re-confirms E31's TICKET-9), Storage FAIL (re-confirms TICKET-10), Providers FAIL (re-confirms TICKET-11; failover chain itself healthy), Runtime PASS (against a freshly-started local instance), Tests 214/214 PASS, overall Validation Ready: NO. New, genuine findings beyond re-confirming E31: RLS enforcement and anon-RPC-execute-denial verified live and correct on every checkable table/RPC (a real, new positive security finding); three migrations (0011, 0013, 0014) violate the project's own RLS-policy idempotency convention (TICKET-12); a prior documentation claim in `20_Engineering/Database_Architecture.md` and `docs/README.md` about a duplicate `0009_` migration prefix was itself false — corrected in both files, traced to an uncross-checked planning-document reference. Full repository audit in `docs/40_Validation/Technical_Debt_Report_2026-08-06.md` (T-1 through T-10, zero Critical findings) and engineering baseline in `docs/40_Validation/Repository_Baseline_2026-08-06.md`. New CI workflow `.github/workflows/ci.yml` (Lint → Unit Tests → Migration Verification → Infrastructure Verification → Deployment → Runtime Verification) — the Deployment stage is honestly incomplete pending a GitHub Secrets / branch-protection decision outside this sprint's reach, documented as such in `docs/40_Operations/Deployment_Verification_Guide.md` rather than pretended complete. Four new `docs/40_Operations/` docs (`Deployment_Verification_Guide.md`, `Infrastructure_Verification.md`, `Runtime_Health_Check.md`, `Recovery_Procedure.md`). No business logic, architecture, or test mocks modified, per this sprint's explicit rules. Recommendation: **Hold**, unchanged in substance from E30/E31, now backed by a repeatable tool instead of one-off checks.	This session, 6 August 2026
E33	Sprint 3.5.6 pre-flight check — `python api/scripts/verify_project.py` run again for real per this sprint's explicit "verify before anything else, stop if it fails" instruction. Result unchanged from E32: Database FAIL (litigation tables/columns still missing, TICKET-9), Storage FAIL (buckets still unprovisioned, TICKET-10), Providers FAIL (Cerebras still invalid, TICKET-11), Environment/Migrations(WARN)/Runtime/Tests unchanged. The sprint's own stated premise ("assume production infrastructure has now been fully provisioned") did not match the real, current state of the production Supabase project. Per the sprint's explicit rule, execution stopped at this point — no scenarios run, no AI Case Analysis calls made, nothing fabricated to fill the gap. No new tickets filed (nothing new found; TICKET-9/10/11 already cover this exactly). No files modified beyond this evidence entry — the verification run itself is read-only.	This session, 7 August 2026
E34	Deployment Recovery Sprint — re-ran `verify_project.py` a third time this session at the user's explicit direction after being told production had been provisioned (0013/0014 applied, both Storage buckets created); result contradicted the claim — identical to E33 (4 litigation tables still missing, both buckets still missing), independently re-confirmed via a fourth, targeted direct check. No execution or infrastructure change followed (this sprint's rules: audit and plan only). Produced five planning deliverables in `docs/40_Validation/`: `Expected_Schema_Inventory_2026-08-07.md` (full schema manifest derived from reading all 14 migrations, including net-of-drops indexes/columns), `Migration_Execution_Checklist_2026-08-07.md` (per-migration dependency/idempotency audit — confirms 0011/0013/0014 share the TICKET-12 unguarded-CREATE-POLICY gap, no migration found to assume an undocumented manual step), `Production_Recovery_Plan_2026-08-07.md` (ordered two-migration + bucket-provisioning procedure with rollback SQL; flags the `evidence` bucket's public-vs-private access as a genuine confidentiality decision point rather than defaulting it silently), `SQL_Verification_Checklist_2026-08-07.md` (verification SQL per step, explicitly split into REST-API-reachable vs. SQL-Editor-only), `Gap_Analysis_2026-08-07.md` (expected-vs-verified, with an honest "unknown, not FAIL" category for index/constraint checks this session's tooling can't reach). No migrations executed, no infrastructure modified, no application code touched.	This session, 7 August 2026
E41	RERA & Real Estate — Phase 1 Backend Implementation. Inspected the repository and found `rera_guides` already existed (migration 0001) but had never been used by any code — no `procedure` column, so only a flat per-state step list, not the multi-procedure model the product needs. Resolved all five documented RERA gaps (complaint endpoint/model, walkthrough progress endpoint/model, state-specific data model, deed template integration, state-selector persistence) as design decisions, not left as future work: (1) property deeds and RERA complaints reuse the existing `contracts.py::generate_draft()` template engine with ZERO new drafting code — proved end-to-end by seeding one real Sale Deed template (8 clauses, 6 fixed_boilerplate/2 llm_fillable) and one real RERA Complaint template (5 clauses, 4 fixed_boilerplate/1 llm_fillable, grounds clause cites only RERA Act 2016 §11(4)(a)/§18 as boilerplate — no LLM call, nothing to invent); (2) walkthrough progress is a new table, `rera_walkthrough_progress`, deliberately user-scoped not matter-scoped (optional `matter_id` association) since the product's own routing carries no matterId segment; (3) `rera_guides` extended additively (`procedure`, `heading`, `required_documents`, `portal_url`, `warnings`, `verification_status`) rather than replaced. New migration `0019_rera_backend.sql` (authored, NOT applied to production this session — a deliberate scope decision to avoid an unreviewed production write; `rera_guides`/`state_rules` confirmed to have zero rows regardless). New router+service (`app/routers/rera.py`, `app/services/rera.py`, 5 endpoints: states/procedures/steps/progress-get/progress-put), all RLS-backed (`rera_walkthrough_progress_owner_all`, direct `user_id = auth.uid()`, same pattern as `matters` itself). No fabricated legal/procedural content anywhere — every walkthrough step the API can return is real curated data or an honest empty list, never a guess; ADR-010's Delhi/Maharashtra/Uttar Pradesh Phase 1 scope enforced by validation (400 for any other state). 20 new backend tests (`test_rera.py`) covering domain logic, validation, cross-user-write-stamping (with an explicit, undisguised note on the limit of what a FakeDB test can prove about RLS-only isolation), and API wiring — all passing. Full backend suite re-run after all changes: **321/321 passing, zero regressions** (11m9s). `app.openapi()` inspected directly: all 5 new `/api/rera/...` paths present with correct methods. Both new docx skeletons (`templates/rera/sale-deed.docx`, `rera-complaint.docx`) built and verified locally. Delivered `docs/30_Implementation/RERA_BACKEND_INTEGRATION_CONTRACT.md` for the separate frontend/UI agent building `/rera/...`. Backend only, per this session's explicit scope — no frontend code touched, no Litigation/Contracts behavior modified, Consulting not started. Known limitations stated plainly in the integration contract's own §11, not glossed over: only 1 of 4 property deed types authored (Gift/Mortgage/Relinquishment are the identical pattern, pure follow-up); zero curated walkthrough content; migration not yet applied to production.	This session, 17 August 2026
E42	Consulting & Legal Research — Phase 1 Backend Implementation. Forensic inventory found no Consulting-specific code existed yet, but the "80% reuse of Litigation engine" design note in `Business_Architecture.md` proved literally true: `matters.module` already accepted `'consulting'` (migration 0001), `MODULE_TASK_TYPE` already mapped it to a `consulting_analyst` LLM Gateway task type (unused anywhere until this sprint), the generic `POST/GET /api/matters/{id}/messages` endpoints already routed Consulting matters through that task type end-to-end for free-form chat, and `limitation.calculate_limitation()`/`forum.determine_forum()` were both already pure functions with zero litigation-matter binding. The one genuine gap was a versioned, grounded, structured analysis artifact — exactly what `litigation_case_analyses`/`case_analysis.py` already solved for Litigation, so this sprint mirrored that pattern directly rather than inventing a new one: new `consulting_analyses` table (migration `0020_consulting_backend.sql`, RLS select+insert-owner via `matters` join, NOT yet applied to production), new `app/services/consulting.py` (deterministic/LLM trust boundary: `applicable_law` entries cross-checked against RAG-retrieved chunks and flagged `grounded`; `correct_forum`/`limitation_period` are the deterministic Forum Advisor/Limitation Calculator result only when the caller supplies one, otherwise an explicitly `deterministic: false` LLM advisory fallback — never presented as equivalent; every `case_law_references` name run through the same Citation Verifier gate as every other module), new `app/routers/consulting.py` (`POST /api/consulting/analyze` — omitting `matter_id` creates a new matter via the existing `matters.create_matter`, no duplicate matter-creation logic; supplying one is a same-matter follow-up, new version, never a new matter; `GET /api/consulting/matters/{id}/analyses`). The `consulting_analyst` system prompt (previously prose-only, unused) was extended to a structured JSON contract, same approach as `case_analyst` — same task_type key reused, no new task type introduced. `llm_gateway.generate_json()` gained an optional `history` passthrough parameter (small, backward-compatible extension) so follow-up questions get conversation context. No fabricated legal content: the RAG retriever answers only from the existing shared statute corpus, and an out-of-corpus question legitimately returns a sparse/empty `applicable_law` rather than a guess. 19 new backend tests (`test_consulting.py`) covering query validation, matter auto-creation, follow-up/same-matter versioning, statute grounding, deterministic-vs-advisory forum/limitation, citation verification, malformed-JSON degradation, provider-failure propagation, and full authorization (owner success, another user denied, wrong-module matter rejected, unknown matter 404, unauthenticated denied) — all passing. Full backend suite re-run after all changes: **355/355 passing, zero regressions** (7m40s). `app.openapi()` inspected directly: both new `/api/consulting/...` paths present with correct methods; pre-existing Contracts/Litigation/RERA routes independently re-confirmed unmodified. Delivered `docs/30_Implementation/CONSULTING_BACKEND_INTEGRATION_CONTRACT.md` for the separate frontend/UI agent building `/consulting/...`. Backend only, per this session's explicit scope — no frontend code touched, no Contracts/Litigation/RERA behavior modified. Known limitations stated plainly in the integration contract's own §8: no curated legal source data added beyond the existing shared corpus; no separate LLM-based Query Validator classification call (served instead by the single analysis call's own `missing_information` field); the litigation-support "strategy brief" half of Module 4 not built.	This session, 17 August 2026
E40	Project Re-Baselining Sprint — closed the Authentication workstream (investigation, logging enhancement, and release integration all confirmed complete on `origin/main` at `02fec96`) and established the current release-readiness baseline. Reviewed `Build_Tracker.md`, `Backlog.md`, `Release_Gates.md`, and `docs/40_Validation/README.md` directly (not from memory) and produced `docs/40_Validation/Project_Baseline_2026-08-10.md`: a milestone summary (Engineering complete; Advocate Review R1 active — package built and pushed, zero fields filled in yet, the actual current bottleneck; Engineering Improvements blocked on R1; Beta Release not started), a full open-ticket classification (15 open tickets sorted into Blocking Advocate Review [none], Blocking Beta [TICKET-8/12/13/16/20/24 plus TICKET-25's one remaining follow-up], Blocking Public Release [TICKET-7], Future Enhancement [the remaining 8]), a 5-stage release-readiness table, and a 5-step roadmap (Advocate Review R1 → Engineering Improvements → Pleading Pipeline Re-Validation → Beta Readiness Review → Beta Release) justified entirely from existing documentation, not invented. No ticket created or closed, no application code/infrastructure/schema touched. One documentation gap surfaced and disclosed, not fixed (out of this sprint's review-only scope for that file): `docs/40_Validation/README.md`'s own round-by-round index still stops at Sprint 3.6 Phase 1, not yet updated for Phase 2/2A, the stabilization commits, R1, or the Authentication work.	This session, 10 August 2026
E39	Authentication Logging Enhancement, deployed to production. Followed a real live-production incident (intermittent 401s, browser CORS messages during a Render redeploy) whose Authentication Investigation Sprint traced the root cause to Render deployment timing plus a stale browser session — not a code defect, but the investigation itself needed a full live-testing pass (real throwaway Supabase Auth user, real Playwright browser session against the live production frontend, direct request-matrix testing against the live backend) to reach that conclusion, because `app/auth.py::get_current_user` logged nothing on any rejection path — the only place a specific reason appeared was the HTTP response body, which Render's stdout capture never included. Added one WARNING-level, secret-free log line per rejection, distinguishing six failure categories: `missing_header`, `malformed_header`, `invalid_jwt`, `session_expired_or_revoked`, `supabase_verification_failed`, `no_user_returned` — the `session_expired_or_revoked` vs. `invalid_jwt` distinction was confirmed live during the investigation via an actual sign-out-then-reuse test against the real Supabase project, not assumed. Never logs the Authorization header, the JWT, a refresh token, or user PII — only category, endpoint, HTTP status, a fixed high-level reason string (never the raw exception text), and a timestamp. **Authentication behaviour is unchanged, verified rather than assumed**: every branch still raises the exact same `HTTPException` (status + detail) it did before; the one addition outside `auth.py` (`app/main.py`, for the one failure category — a truly missing header — that never reaches `get_current_user` at all, since FastAPI's own `Header(...)` validation rejects it first with a 422) always delegates to FastAPI's own default `request_validation_exception_handler`, so that response is byte-identical too. New `api/tests/test_auth.py` (17 tests) exercises the real `get_current_user` directly — unlike every other test in this suite, which overrides the dependency wholesale — specifically to prove byte-level status/detail parity alongside the new logging, plus asserts a real secret string never appears in any log record. Full suite 279/279 passing (262 pre-existing + 17 new), re-verified independently at both the Authentication Logging Enhancement sprint and this release sprint. Committed `61e695e`, pushed to `origin/main`, confirmed identical to `origin/main` post-push (not assumed from a non-error `git push` exit code). No CORS, JWT, session, Supabase configuration, or frontend code touched.	This session, 10 August 2026
E38	Sprint 3.6 Phase 2A — Legal Grounds Intelligence, executed. Scoped narrowly per this sprint's explicit brief: not a pleading-functionality sprint, a reliability sprint targeting TICKET-25 specifically (matching Advocate Review R1's [E37] own provisional Phase 2A scope recommendation). **Root cause found with live evidence, not inferred**: regenerating Phase 2's original malformed sample live (never persisted at the time) surfaced a literal unescaped newline inside a JSON string value — not a structural JSON error. A targeted follow-up reproduced the exact trigger 3/3 times on the same matter/model: the model copying a text span across an ambiguous prompt-section boundary (including the literal blank-line separator) into a field value. **Pipeline redesigned, not just re-prompted**: `legal_grounds` restructured from one free-form "content" string into a structured, per-issue "grounds" list (`{issue, statute_refs, case_law_refs, argument_note, confidence}`, no migration needed — existing `content` JSONB column), with a genuinely new live Citation Verifier stage for any case name it proposes that isn't already in the outline's verified pool (bounded, 3 live IK calls/generation) — the concrete mechanism behind investigating TICKET-25's sibling finding, zero case-law ever proposed anywhere in this codebase, confirmed this sprint to be systemic (0/6 matters at BOTH the case_analysis AND pleading_outline stages, across a full model-tier mix — no case-law retrieval context is ever given to the model at either upstream stage, so a model correctly following its own "don't guess without a source" instruction proposes nothing). Two gateway-level structural defenses added, benefiting all 5 LLM clause types (`llm_gateway.py`): native provider JSON-mode (`generate_json()`) and one automatic, PII-safe repair retry (never threads unmasked model output back into a new outbound prompt). **Regression** (same 6 real certification matters, real persisted generations, Phase 2's own methodology): legal_grounds malformed rate 33%→0% (target <5%, met), statute-citation-attempt rate 50%→67%, composition 4/6→6/6 clean, 0 ungrounded statute claims/0 fabricated case-law names. Precedent usage unchanged at 0/6 verified — the new live-verification mechanism is correctly wired and unit-tested but was not exercised into a positive result this session (Groq's models, the only tier reachable after Gemini's session-wide quota exhaustion, did not propose case names even when invited) — disclosed as a real limitation, not glossed over. 8 new/rewritten backend tests (`test_llm_gateway.py` +7, `test_clause_generator.py` net +8, `test_document_composer.py` mock-signature update); full suite 262/262 passing (plus the same pre-existing, unrelated e2e Playwright gap every prior session has noted). No migration required. New reusable diagnostic tooling added to `api/scripts/` (`diagnose_legal_grounds.py`, `diagnose_legal_grounds_flash_lite.py`, `diagnose_legal_grounds_postfix.py`, `regress_legal_grounds.py`), matching this project's `verify_*.py` convention. TICKET-24/26/27 left open per the brief's own "only if they naturally fall out of the redesign, do not optimize prematurely" instruction — TICKET-26 got an incidental partial note (legal_grounds's own citation rate improved as a side effect; the other two LLM clause types were not touched). Recommendation: **FURTHER LEGAL-GROUNDS WORK REQUIRED** — narrower than Phase 2's finding: TICKET-25 is substantially resolved pending one follow-up (`gemini-2.5-flash-lite`-targeted re-confirmation once quota allows — the tier responsible for 100% of known failures to date, not reachable this session), and precedent/case-law usage remains the one real, undemonstrated gap. See `docs/40_Validation/Sprint_3.6_Phase2A_Legal_Grounds_Report_2026-08-09.md`. No commit made this session, per this session's standing rule.	This session, 9 August 2026
E37	Review Milestone R1 — Advocate Validation, prepared. Not an engineering sprint — no code, migration, prompt, model-tuning, or corpus change was made, per this milestone's explicit rules. Built a complete, self-contained legal review package for Advocate Nitesh under `docs/40_Validation/Advocate_Review_R1/`, assembled entirely from real, already-generated Sprint 3.6 Phase 2 output (read-only queries against the live production Supabase project — no new AI generation): 6 per-matter Advocate Review Packages (matter summary, parties, chronology, evidence, AI Case Analysis, Pleading Outline, all 14 generated clauses with version/confidence/citation detail and composed-draft status, matter-specific known limitations); a Clause Review Questionnaire (84 per-clause rows + 6 Overall Pleading Assessments + a Cross-Matter Reflection); a Feedback Capture Template (Matter/Clause/Version/Reviewer/Severity/Finding/Suggested-improvement, directly convertible to `Backlog.md` tickets, next ticket number TICKET-28); a Review Dashboard (system-side metrics real and pre-filled — 84/84 clauses generated, 82/84 succeeded, 4/6 matters composed a complete draft, 0.89 average AI confidence on composed clauses — legal-quality columns intentionally blank pending Nitesh's review); an Engineering Backlog Mapping (Critical/Major/Minor/Enhancement classification framework, a known-issues-by-clause-type table cross-referencing TICKET-16/24/25/26/27 to avoid duplicate filing on rediscovery); and a facilitation-guide README with a provisional Sprint 3.6 Phase 2A recommendation. **Building the review packages surfaced two new, real, live confirmations of already-filed defects, found and documented (not fixed) in the process**: TICKET-24's PII-placeholder leak (`PARTY_I1`) is visible not just in clause_generator.py's own output but one layer further upstream, in the Pleading Outline's `evidence_mapping` field itself (Phase 1 code) — confirmed directly in CIV-01's real data; and two additional live instances of a plausibly-correct-but-unretrieved statute citation (IA-01's regenerated Facts clause citing CPC §151/Order XXXIX Rules 1&2; PROP-03's Reliefs clause citing Specific Relief Act §10), both tracing to the same already-open TICKET-16 corpus-recall gap, not new root causes. No new tickets filed this session — both findings are documented in the review packages and `Engineering_Backlog_Mapping.md` for Nitesh's awareness and Sprint 3.6 Phase 2A triage, consistent with existing TICKET-16/24 rather than duplicating them.	This session, 9 August 2026
E36	Sprint 3.6 Phase 2 — Clause-Based Drafting Engine, executed. Built on top of Phase 1's Pleading Outline (E-equivalent in `Sprint_3.6_Phase1_Foundation_Report_2026-08-09.md`), per this sprint's explicit brief: 14 independent, independently versioned, independently regenerable clause generators (`api/app/services/clause_generator.py`, migration `0018_pleading_clauses.sql`), a reasoning-free document composer that assembles only advocate-approved clause versions in fixed order (`api/app/services/document_composer.py`), and a Human Review gate (`review_clause()`) the composer cannot bypass. 9 of 14 clause types are deterministic templating (zero LLM calls — Jurisdiction was reclassified from an initial LLM assumption to deterministic during design, since the Forum Advisor's output is already fully structured); 5 genuinely need free-prose synthesis (Facts, Cause of Action, Legal Grounds, Reliefs, Prayer) and share one new LLM Gateway task type (`clause_drafter`). Every LLM clause's statute/case-law claims are cross-checked against the SAME data its source Pleading Outline already retrieved/verified — never re-retrieved, never a freshly-proposed unverified case name accepted (Hard Rules 1/3, same trust boundary as every prior AI artifact in this pipeline). 28 new backend tests (20 `clause_generator`, 8 `document_composer`); full suite 253/253 passing. Migration `0018` applied to the real production Supabase project via `supabase db query --linked` (same sanctioned path as E35), both new tables confirmed present by live schema query. **Live evaluation executed against the same 6 real certification matters Phase 1 used** (real LLM calls, real Citation-Verifier-gated data, real 293-second run): 84/84 clause generations completed with zero hard errors; 4/6 matters composed a complete 14-section draft with zero missing clauses on the first pass. Found and documented (not fixed, per this sprint's Defect Policy) four new findings: **TICKET-24** (Major — a real PII-mask placeholder leaked into final unmasked clause text in one live run, non-deterministic, same root cause as the already-known TICKET-15 but a more severe manifestation), **TICKET-25** (Major — `legal_grounds`, the clause most needing statute/precedent grounding, had a 33% malformed-output rate, the worst of the 5 LLM clause types), **TICKET-26** (Minor — most LLM clauses that could plausibly cite something claimed zero refs, a coverage gap, not an incorrect-grounding one), **TICKET-27** (Minor — clause regeneration is not guaranteed to reuse the same model tier). Also directly, quantitatively confirmed and sharpened Phase 1's TICKET-21 capacity concern: 100% of this round's 30 LLM clause calls degraded past `gemini-2.5-pro` (which served zero of them), with a visibly worsening fallback-tier pattern observable within the single 293-second run itself, not just across a whole session. Recommendation: **CLAUSE ENGINE REQUIRES FURTHER WORK** — architecture solid and live-validated, but not yet the foundation for full pleading drafting until the Legal Grounds reliability gap is diagnosed, Phase 1's case-law-recall gap closes enough to let this module actually demonstrate precedent grounding (0/24 opportunities this round, entirely because upstream data was empty — correct behavior, not yet validated capability), and free-tier capacity under realistic per-matter call volumes is better understood. See `docs/40_Validation/Sprint_3.6_Phase2_Clause_Engine_Report_2026-08-09.md`. No commit made this session, per this session's standing rule.	This session, 9 August 2026
E35	Sprint D1 — Production Synchronization, executed. Applied migrations `0013_litigation_schema.sql` and `0014_litigation_case_analysis.sql` against the real Supabase project (`pgwemjswxdlnshrfoggj`) via `supabase db query --linked` (Management API — the sanctioned path found this session, since no raw Postgres password/DATABASE_URL exists in this environment); every table/column/index/RLS policy confirmed present by direct SQL re-query against the live catalog, not assumed from a non-error exit code. Created both Storage buckets; per the sprint's explicit stop-and-decide gate, the `evidence` bucket's public-vs-private access was presented as Option A (public, matches the already-shipped `get_public_url()` code, ships today) vs. Option B (private/signed-URL, stronger confidentiality, needs a code change out of this sprint's scope) — user approved Option A, and TICKET-13 was filed immediately as the tracked path to Option B. `verify_project.py` re-run: Database PASS, Storage PASS (real upload/download round-trip against the live `evidence` bucket), Providers FAIL (TICKET-11/Cerebras only, judged out of scope for this sprint), Environment/Runtime PASS, 214/214 tests. Then ran the sprint's required smoke test (real Supabase Auth user, real JWT, real HTTP calls against a local server pointed at production) — Matter/2 Parties/Fact/Evidence upload/Limitation/Forum all succeeded, but `POST .../case-analysis` returned a real HTTP 500 after ~61s. Diagnosed from the server's actual traceback (not guessed): `pii_masks` RLS rejection, filed and fixed as **TICKET-14** (see Backlog.md — `case_analysis.py` was reusing the RLS-scoped `user.db` for a table that has zero RLS policies by design and is reachable only via `service_client()`; one-line fix, mirrors the already-correct pattern in `matters.py`). Fix approved by the user as a scoped exception to "no feature development" given it left AI Case Analysis completely non-functional in production. Re-ran the full smoke test end-to-end after the fix: `201 Created`, real Gemini-generated analysis with correctly round-tripped party names, `litigation_case_analyses` row and 9 `pii_masks` rows both confirmed persisted via direct service-role query. A secondary, unfixed observation (PII auto-detector over-masking some non-name tokens) filed as **TICKET-15** (Minor). All test data from both smoke-test runs (2 matters, cascade-deleted rows, 1 storage file, 2 throwaway auth users) cleaned up and independently re-verified as removed; local smoke-test server processes killed. Full backend suite re-run clean at 215/216 (214 unit/integration + 1 unrelated pre-existing e2e Playwright failure requiring a frontend dev server not started this session, confirmed unrelated to this fix by reading its own traceback). No commit created this session — the `case_analysis.py`/`db.py` fix and all migration/infrastructure changes remain uncommitted pending the user's explicit instruction to commit.	This session, 8 August 2026
---
Document version: v25 — 17 August 2026
Change from v24: recorded Evidence Log item E41 — RERA & Real Estate Phase 1 backend implementation. Resolved all five previously-documented RERA gaps as explicit design decisions: property deeds/complaints reuse the existing Contracts template engine (one Sale Deed + one RERA Complaint template seeded, zero new drafting code), a new user-scoped `rera_walkthrough_progress` table + 5-endpoint router/service for state/procedure/step/progress, and an additive extension of the long-dormant `rera_guides` table. Migration `0019` authored but not applied to production. 20 new tests, full suite 321/321 passing. No frontend built. See `docs/30_Implementation/RERA_BACKEND_INTEGRATION_CONTRACT.md`.
Change from v23: recorded Evidence Log item E40 — Project Re-Baselining Sprint. Authentication workstream formally closed. New `docs/40_Validation/Project_Baseline_2026-08-10.md` establishes the current release-readiness baseline: Engineering complete, Advocate Review R1 active (the current bottleneck), Engineering Improvements blocked on R1 feedback, Beta Release not started. 15 open Backlog.md tickets classified by release-blocking tier; none block Advocate Review. Roadmap: R1 -> Engineering Improvements -> Pleading Pipeline Re-Validation -> Beta Readiness Review -> Beta Release.
Change from v22: recorded Evidence Log item E39 — Authentication Logging Enhancement deployed to production (`61e695e`). No authentication, authorization, JWT, session, Supabase, CORS, or frontend behavior changed — six failure categories now logged (secret-free) in `app/auth.py`/`app/main.py`, closing the diagnostic gap the same day's Authentication Investigation Sprint found. 279/279 tests passing, behavior parity verified via 17 new tests exercising the real auth dependency directly. Pushed and confirmed identical to `origin/main`.
Change from v21: recorded Evidence Log item E38 — Sprint 3.6 Phase 2A (Legal Grounds Intelligence). TICKET-25 root-caused with live, reproduced evidence (a prompt section-boundary ambiguity causing the model to copy a text span, including a literal newline, into a JSON string field) and substantially resolved: legal_grounds redesigned into a structured per-issue "grounds" list plus a new live case-law verification stage, gateway-level JSON-mode + one repair retry added for all 5 LLM clause types. Regression on the same 6 real certification matters: malformed rate 33%→0% (target met), citation-attempt rate 50%→67%, composition 4/6→6/6 clean. Precedent usage unchanged (0/6) — the case-law-context gap found to be systemic across the whole codebase, a new mechanism added but not yet exercised into a positive result. 262/262 backend tests passing. Recommendation: FURTHER LEGAL-GROUNDS WORK REQUIRED (narrower than Phase 2's finding — one follow-up re-confirmation + precedent usage are the two remaining gaps). See `docs/40_Validation/Sprint_3.6_Phase2A_Legal_Grounds_Report_2026-08-09.md`.
Change from v20: recorded Evidence Log item E37 — Review Milestone R1 (Advocate Validation), a documentation/facilitation milestone, not an engineering sprint. Prepared a complete legal review package for Nitesh (`docs/40_Validation/Advocate_Review_R1/`): 6 per-matter review packages, a clause-by-clause questionnaire, a feedback capture template convertible to backlog tickets, a review dashboard with real system metrics and blank legal-quality columns, an engineering backlog mapping/classification framework, and a facilitation guide with a provisional Phase 2A recommendation. No code/migration/prompt/model changes. Surfaced two new live confirmations of already-open defects (TICKET-24 visible one layer further upstream than previously known; two new instances of TICKET-16's retrieval gap) while assembling the packages — documented, not fixed.
Change from v19: recorded Evidence Log item E36 — Sprint 3.6 Phase 2 Clause-Based Drafting Engine. 14 clause generators (9 deterministic, 5 LLM), a reasoning-free document composer, and a Human Review gate built on top of Phase 1's Pleading Outline; migration 0018 applied to production; 253/253 backend tests passing (28 new). Live-evaluated against the same 6 real Phase 1 certification matters — 84/84 clause generations succeeded with zero hard errors, 4/6 matters composed complete drafts. Filed TICKET-24 (PII placeholder leak, Major), TICKET-25 (Legal Grounds unreliability, Major), TICKET-26 (grounding coverage gap, Minor), TICKET-27 (regeneration model-tier inconsistency, Minor) — none fixed this sprint per the Defect Policy. Sharpened Phase 1's TICKET-21 capacity concern with a within-single-run degradation pattern. Recommendation: CLAUSE ENGINE REQUIRES FURTHER WORK. See `docs/40_Validation/Sprint_3.6_Phase2_Clause_Engine_Report_2026-08-09.md`.
Owner: Keshav
Change from v18: recorded Evidence Log item E35 — Sprint D1 executed: migrations 0013/0014 applied and verified against real production Supabase, both Storage buckets created (evidence bucket public, per approved Option A; TICKET-13 filed for the future private/signed-URL migration), verify_project.py re-run (Database/Storage/Environment/Runtime/Tests PASS, Providers FAIL on pre-existing TICKET-11 only). The sprint's required smoke test found a real, previously-undiscovered production bug (TICKET-14, pii_masks RLS rejection breaking AI Case Analysis end-to-end) via a genuine HTTP 500 and its real server traceback — fixed same day at the user's explicit approval, re-verified with a second successful smoke test, all test data cleaned up. TICKET-9/10 now closed (this entry supersedes them); TICKET-11/12/13/15 remain open.
Change from v17: recorded Evidence Log item E34 — Deployment Recovery Sprint. A claimed "production now fully provisioned" state did not match a third independent verification; produced an audit-and-plan (not execution) response: full expected-schema inventory, migration dependency/idempotency audit, ordered recovery plan with rollback SQL, SQL verification checklist, and gap analysis. TICKET-9/10/11/12 unchanged, all still open.
Change from v16: recorded Evidence Log item E33 — Sprint 3.5.6's pre-flight verification re-run, confirming TICKET-9/10/11 remain open and unfixed, contradicting that sprint's stated assumption. Execution correctly stopped there per the sprint's own rule.
Change from v15: recorded Evidence Log item E32 (Sprint 3.5.5B) — the infrastructure verification framework, its real findings (re-confirming TICKET-9/10/11, adding TICKET-12, correcting a false 0009-migration-duplicate claim, confirming RLS/RPC security posture live), the new CI workflow, and four new Operations docs. Hold recommendation stands.
Change from v14: updated §9 (Open Decisions) to record Evidence Log item E31 — Sprint 3.5.5A found real, working credentials at the monorepo-root `.env` (correcting E30's incomplete check), then found the actual blocker: Litigation migrations 0013/0014 were never applied to the real Supabase project, and no storage buckets are provisioned. Filed as TICKET-9/10/11. Phases 2-6 not attempted; Hold recommendation stands for a more precise reason.
Change from v13: updated §9 (Open Decisions) to record Evidence Log item E30 — the Sprint 3.5.5 Product Validation round, executed for the deterministic layer only (no live LLM/Supabase credentials in this environment), resulting in a Hold recommendation for AI Pleading Generation pending full validation. See `docs/40_Validation/`.
